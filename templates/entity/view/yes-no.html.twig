<div class="badge rounded-pill text-bg-{{ (data ? 'primary' : 'secondary') }} w-100 text-start"><i class="fas fa-{{ (data ? 'check' : 'times') }} me-1"></i>{{ data|label_from_option(field.choices)|trans }}</div>