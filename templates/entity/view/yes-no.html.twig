<div class="badge badge-pill badge-{{ (data ? 'primary' : 'secondary') }} w-100 text-left"><i class="fas fa-{{ (data ? 'check' : 'times') }} mr-1"></i>{{ data|label_from_option(field.choices)|trans }}</div>